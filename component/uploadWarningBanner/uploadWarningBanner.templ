package uploadWarningBanner

templ Warning() {
	<div class="px-3 py-2 text-center text-red-700 bg-red-200 border-t border-b border-red-500" role="alert">
		<p class="text-xl font-bold">Warning</p>
		<p class="text-sm">All files uploaded here are hosted <i>publically</i>. <span class="font-bold">Do <u>NOT</u> upload files containing sensitive data or secrets.</span></p>
	</div>
}
